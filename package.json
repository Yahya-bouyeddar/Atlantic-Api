{
  "name": "bon-de-coulage-api",
  "version": "1.0.0",
  "description": "API pour générer des BON DE COULAGE en PDF avec Puppeteer",
  "main": "server.js",
  "node": ">=20.10.0 <21.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild server.js --bundle --platform=node --outfile=dist/server.js --external:puppeteer --external:puppeteer-core --external:@sparticuz/chromium-min --external:typescript"

  },
  "keywords": [
    "pdf",
    "bon-de-coulage",
    "express",
    "puppeteer",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "path": "^0.12.7",
    "puppeteer": "^21.5.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "nodemon": "^3.0.1"
  }
}
